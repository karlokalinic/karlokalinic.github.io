# Repository Agent Map

This repository contains multiple unrelated projects. Do not assume repository-wide architectural rules apply everywhere.

For KARLOLEGEND Desktop work under `karlolegend-desktop/**`:
1. Read `karlolegend-desktop/AI_CONTEXT.md`.
2. Read the nearest `AGENTS.md` under `karlolegend-desktop/`.
3. Use `karlolegend-desktop/source/docs/` as the detailed system of record.
4. Never modify the unrelated GitHub Pages/site files unless the task explicitly requires it.

Keep agent onboarding concise. Do not duplicate the entire documentation into prompts; follow links from `AI_CONTEXT.md` only as needed.
